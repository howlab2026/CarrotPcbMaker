{
  "name": "pcb-design-club",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server start",
    "dev:client": "npm --prefix client run dev",
    "deploy": "node scripts/deploy.js"
  }
}
